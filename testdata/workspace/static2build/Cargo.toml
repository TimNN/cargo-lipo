[package]
name = "static2build"
version = "0.1.0"
authors = ["Tim Neumann <mail@timnn.me>"]
edition = "2018"

[lib]
crate-type = ["staticlib"]

[build-dependencies]
buildutil = { path = "../buildutil" }
